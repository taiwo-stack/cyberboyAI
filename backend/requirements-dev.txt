# Install alongside requirements.txt for development and CI:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0
httpx==0.27.0      # needed for TestClient / ASGITransport in test_api.py
ruff==0.4.9        # linter
