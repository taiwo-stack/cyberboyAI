[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Show short tracebacks for faster CI output; use --tb=long locally if needed
addopts = -v --tb=short
